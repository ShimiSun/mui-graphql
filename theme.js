import { createMuiTheme } from "@material-ui/core/styles";

// https://codesandbox.io/s/bd2el?file=/demo.js:86-138
export const scubaTheme = createMuiTheme({
  palette: {
    primary: {
      // Deep blue
      main: "#000851",
    },
    secondary: {
      // Soft blue
      main: "#ff8c69",
    },
  },
});
